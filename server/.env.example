DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_MODEL=deepseek-v4-flash

# 默认关闭第三方 trvl MCP，仅在确认后开启
ENABLE_TRVL_MCP=true

# 两层记忆：Redis 作为热数据缓存；不可用时自动回退到本地 JSON
REDIS_ENABLED=true
REDIS_URL=redis://localhost:6379/0
